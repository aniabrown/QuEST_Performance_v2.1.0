\documentclass{article}
\usepackage{graphics}
\usepackage{tikz}
\pgfrealjobname{survey}
\usepackage{pgfplots}
\pgfplotsset{compat=1.13}

\begin{document}

\newcommand{\arcusResults}[2]{../../arcus-b_CPU/#1qubits#2ranks16threads/TIMING_STATS_ROTATE_#1qubits_CPU_#2ranksx16threads.csv}

% -----------------------------------------------------------------------------
% Arcus weak scaling
% -----------------------------------------------------------------------------

\beginpgfgraphicnamed{arcus_weakScaling}
\begin{tikzpicture}[scale=1.2]
    \begin{axis}[
        xlabel={Target qubit},
        ylabel={Time (s)},
        legend style={at=({0.05,0.40}),anchor=south west},
        very thin,
        %xticklabels = {$2^0$, $2^1$, $2^2$, $2^3$, $2^4$, $2^5$, $2^6$},
        width = 10cm,
        height = 6cm,
        %yticklabels = {$2^0$, $2^1$, $2^2$, $2^3$, $2^4$, $2^5$, $2^6$},
        ]

    \addplot [
        mark=diamond*, mark size=2,
        green,
        error bars/.cd,
        y dir = both, y explicit,
        ]
    table [
        y=time(s),
        col sep=comma,
        y error plus=maxDelta,
        y error minus=minDelta
    ] {\arcusResults{32}{4}};

    \addlegendentry{32 qubits, 4 nodes}


    \addplot [
        mark=diamond*, mark size=2,
        blue,
        error bars/.cd,
        y dir = both, y explicit,
        ]
    table [
        y=time(s),
        col sep=comma,
        y error plus=maxDelta,
        y error minus=minDelta
    ] {\arcusResults{33}{8}};

    \addlegendentry{33 qubits, 8 nodes}

    \addplot [
        mark=diamond*, mark size=2,
        orange,
        error bars/.cd,
        y dir = both, y explicit,
        ]
    table [
        y=time(s),
        col sep=comma,
        y error plus=maxDelta,
        y error minus=minDelta
    ] {\arcusResults{34}{16}};

    \addlegendentry{34 qubits, 16 nodes}

   \addplot [
        mark=diamond*, mark size=2,
        gray,
        error bars/.cd,
        y dir = both, y explicit,
        ]
    table [
        y=time(s),
        col sep=comma,
        y error plus=maxDelta,
        y error minus=minDelta
    ] {\arcusResults{35}{32}};

    \addlegendentry{35 qubits, 32 nodes}

    \addplot [
        mark=diamond*, mark size=2,
        red,
        error bars/.cd,
        y dir = both, y explicit,
        ]
    table [
        y=time(s),
        col sep=comma,
        y error plus=maxDelta,
        y error minus=minDelta
    ] {\arcusResults{36}{64}};

    \addlegendentry{36 qubits, 64 nodes}



\end{axis}
\end{tikzpicture}
\endpgfgraphicnamed


\end{document}

